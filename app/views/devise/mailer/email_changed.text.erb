<%= t("devise.mailer.email_changed.greeting", recipient: @email) %>

<% if @resource.try(:unconfirmed_email?) %>
  <%= t("devise.mailer.email_changed.message", email: @resource.unconfirmed_email) %>
<% else %>
  <%= t("devise.mailer.email_changed.message", email: @resource.email) %>
<% end %>
